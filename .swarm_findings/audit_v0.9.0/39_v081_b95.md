# 39 — v0.8.1 B-95 opponent score-urgency tracking — verify

Commit: `ed46896` (Tue 2026-05-05). Tag v0.8.1. 319/319 tests claimed; on
re-run 330/330 pass at HEAD (suite has grown).

## What B-95 does

Closes wave8 B-95 gap: bot's *own* urgency was modelled (`scoreUrgency`,
`matchPointUrgency`); opponent's was not. Saudi pros bid weaker hands when
desperate ("Hail-Mary") — bot now anticipates this and counter-Bels.

Three pieces:

1. New `opponentUrgency(oppSeat)` (Bot.lua:918) — mirror of `scoreUrgency`
   read from `oppSeat`'s POV. Returns +12 / +6 / -8 / 0.
2. Public `Bot.OpponentUrgency(oppSeat)` wrapper (Bot.lua:934) — exported
   for cross-module use.
3. Two consumers:
   - `Bot.PickDouble` (Bot.lua:3167) — `th = th - 5` if bidder urg ≥ 6.
   - `BotMaster.sampleConsistentDeal` (BotMaster.lua:408) —
     `pickProb = min(pickProb, 0.5)` when sampling bidder's hand if urg ≥ 6.

## 1. Ledger key — written/read correctly?

The "ledger" is `S.s.cumulative` (per-team cumulative score), not a new
key. Helper reads `S.s.cumulative[oppTeam]` and `[other]` with `or 0`
fallback. State.lua:63/330/435 initializes it to `{A=0, B=0}` and reloads
from save fields 16/17. Read path is correct.

## 2. Tier gating — M3lm+ only?

Yes. `opponentUrgency` self-gates with `if not Bot.IsM3lm() then return 0`
(line 919). PickDouble caller also re-checks `Bot.IsM3lm()` (defensive
double-gate, harmless). BotMaster sampler uses the public
`B.Bot.OpponentUrgency` wrapper, which inherits the self-gate.
M3lm requires `WHEREDNGNDB.m3lmBots == true`.

## 3. Edge — cumulative=0/0 at game start

`opp_cum=0`, `our_cum=0`, `target=152`. None of the three branches fire:
`0 ≥ 127`? No. `0-0 > 80`? No. Returns 0. **No spurious fire.** Safe.

## 4. Edge — opp at clinch (≥140 cumulative)

If `opp_cum ≥ 127`, returns `-8` (first branch). Caller checks `bidUrg >= 6`
— so threshold is **NOT** lowered (no Hail-Mary assumption when opp is
*winning*). Sampler also doesn't damp pickProb. Correct asymmetric handling.

## 5. Tests — regression pin?

**No B-95-specific fixture.** Grep for "B-95" / "opponentUrgency" /
"OpponentUrgency" across `Tests/` returns zero. CHANGELOG admits this:
"unit tests for `Bot.PickPlay` legality sweep across many random states
without explicit B-95 fixtures. The behavior is graceful — when
`opponentUrgency` returns 0 (neutral), all wires no-op identically."

**Gap**: regression pin for the +12 / -8 / cap-floor branches absent.

## Conclusion

Implementation is correct, well-gated, edge-safe, no-op when neutral. Floor
`BOT_BEL_TH - 16` (line 3196) absorbs the -5 stack. Risk is low; coverage
gap noted but graceful degradation makes it acceptable.
