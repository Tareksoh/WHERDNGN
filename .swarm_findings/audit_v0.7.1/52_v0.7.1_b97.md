# Audit: v0.7.1 B-97 opp-meld suit avoidance

HEAD = v0.7.2. Scope: `Bot.lua:1655-1683` (commit edeab26 Bot.lua +30, no test diff).

## 1. Authoritative source

`Bot.lua:1664-1666` reads `S.s.meldsByTeam[oppTeam]` directly,
**not** OnPlayObserved / play-history inference. This is the
**authoritative** declared-meld store: written by `S.ApplyMeld`
(`State.lua:1103-1116`) on wire receipt, gated by trick-1-only
(`State.lua:1108`), and idempotent-deduped per (seat, kind, top,
suit). `R.ScoreRound` (`Rules.lua:598-617`), `Net.lua` belote
checks, and `BotMaster.lua:243-246` rollouts all consume the same
table — single source of truth. **Confirms 13_v0.6_v0.7.1_sampler.md
line 8-14.**

## 2. Heuristic correctness

Logic at `Bot.lua:1669-1681`: filter `m.kind:sub(1,3) == "seq"`
matches `seq3`/`seq4`/`seq5` written at `State.lua:1122-1124`. Sets
`fzlokyAvoidSuit = m.suit` only if not already set, then `break` —
single hint, Fzloky-priority preserved. The avoidance feeds into the
existing `fzlokyAvoidSuit` consumer downstream (no separate code
path). Heuristic rationale (opp seq → opp has run in suit → leading
gives them tempo) is sound.

## 3. Belote (Hokm K+Q trump)

**Belote is NOT in `meldsByTeam`** — it's a runtime bonus tracked by
the K+Q-of-trump play sequence, not declared via `S.ApplyMeld`
(no `kind = "bel"` writer found anywhere in State.lua / Net.lua).
`State.lua:1122-1135` only writes `seq3/seq4/seq5/carre` kinds.
Even if a hypothetical belote entry existed, line 1670 explicitly
excludes `m.suit == contract.trump`. **Trump suit is never avoided.
Safe.**

## 4. Sun + opp Carré of Aces (الأربع مئة)

`m.kind:sub(1,3) == "seq"` rejects `kind == "carre"` (prefix
"car"). Per the comment at line 1667-1668: carrés are 4-of-a-rank
across all four suits — no suit-lead implication. **Carré of Aces
in Sun does NOT trigger avoidance.** Correct: the meld value (200)
flows through scoring (`State.lua:1129`, `K.MELD_CARRE_A_SUN`) but
gives no suit-distribution information.

## 5. Tier gating

`Bot.IsM3lm() and not isBidderTeam` at `Bot.lua:1664`. M3lm+ matches
the design rationale ("relies on accumulated meld observations").
`isBidderTeam` derived from contract type + bidder seat at line
1437-1438. Defender-only restriction is sensible — bidder-team lead
selection has stronger drives (draw trump, etc.) that shouldn't be
overridden by defender-style opp-meld avoidance. **Gating correct.**

## Verdict

**PASS.** All five checkpoints clean. Source is authoritative,
heuristic is correct, Belote/Carré edge cases handled by structure
(not in meldsByTeam) and explicit filter (`sub(1,3) == "seq"`),
trump excluded by `m.suit ~= contract.trump`, M3lm+ gating matches
design. No defects. 291/291 tests pass; commit added no new tests.
