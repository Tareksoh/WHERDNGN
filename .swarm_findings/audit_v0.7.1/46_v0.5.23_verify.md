# v0.5.23 verify — Section 1 deferred bidding rules + audit H-3/H-7

**Commit:** `745cfb5` (2026-05-05). Diff: `Bot.lua +68/-6`,
`CHANGELOG.md +78`. No `Rules.lua` or test deltas.

## 1. Section 1 deferred rules — which landed

CHANGELOG/commit body lists four candidates: **B-3, B-7, G-2, G-4**
(plus H-9 as a non-Section-1 deferral).

| ID  | Status        | Wire                                           |
|-----|---------------|------------------------------------------------|
| B-7 | LANDED        | `thSun += 8` when `S.s.cumulative[myTeam] >= K.SUN_BEL_CUMULATIVE_GATE` (=100) — Bot.lua:1003 |
| B-3 | "partial"     | No code. Claims v0.5.19 trick-3 sweep covers 5+ trump Kaboot |
| G-2 | "already wired" | No code. Claims `TH_HOKM_R1_BASE > TH_HOKM_R2_BASE` from v0.5.13 satisfies it |
| G-4 | DEFERRED      | Conflicts with Sun-overcall (later v0.7.0)     |

**B-7 vs decision-trees.md row 53** (`docs/strategy/decision-trees.md:53`):
the row says "Cumulative score ≥100 (Sun-Bel-gate context) … defenders
at <100 may Bel you … Bel-fear band 120-vs-90 … `not yet wired`". The
v0.5.23 wire reads **OUR** team's cumulative and biases `thSun` UP — i.e.
"if WE'RE at ≥100 the OPPONENT (still <100) may Bel us." This matches the
row's logic (per the v0.5.9 E-1 rule, only the team <100 may Bel; opp
<100 still qualifies). +8 nudge is one strength-tier. Wire is correct.

**G-2 / B-3** are documentation-only claims; no diff exercises them in
v0.5.23. Cannot verify in this commit alone.

## 2. H-3 audit fix — what was H-3, what was the issue

The CHANGELOG/commit explicitly cites **MASTER_REPORT.md H-3 / wave3 A-47**,
which is a *different* H-3 from the v0.5.0 sampler-bias H-3 — note the
ID collision in CHANGELOG (`v0.5.0 H-3 = sampler partner-trump bias`,
v0.5.23 H-3 = `singleton-lead rank guard`).

**Audit issue (MASTER_REPORT.md:76-82):** `pickLead` priority 2 picked
the lowest singleton **unconditionally**, including singleton A/T/K/Q in
Hokm — opponent void in that suit over-ruffs and captures the honor.

**Fix (Bot.lua:1813-1844):** in Hokm only, filter singletons to ranks
`7/8/9`; if all singletons are honors, fall through to longest-suit-low.
Sun unchanged (A/T are sure stoppers — no trump). Implementation matches
audit fix recommendation.

## 3. H-7 audit fix — what was H-7, what was the issue

H-7 in this audit is **NOT** the v0.5 "Sun shortest-suit lead" referenced
in `Bot.lua:1846` legacy comment. It is **MASTER_REPORT.md:108-114 / wave2
A-56**: `scoreUrgency` (+12 max) + `matchPointUrgency` (±10) summed
**uncapped** to ±22, dropping `BOT_BEL_TH=70` to 48 — bot Bels garbage
when desperate. Comment intent was "combined cap ±15".

**Fix (Bot.lua:839-852):** new `combinedUrgency(team, context)` helper
clamps SUM to `[-15, +15]`. Routed through 5 callsites: PickBid R1/R2
(line 957), PickDouble (2643), PickTriple (2722), PickFour (2754),
PickGahwa (2802). All five replaced; no stragglers.

**Note:** user prompt mislabels both — H-3 originally was sampler
partner-trump bias (v0.5.0) and H-7 originally was Sun shortest-suit
lead. v0.5.23 reuses the IDs for unrelated MASTER_REPORT items.
