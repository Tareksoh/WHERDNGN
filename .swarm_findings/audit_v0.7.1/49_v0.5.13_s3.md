# v0.5.13 verify — S-3 calibration + magic-number K.* promotion

Commit `bc98b8e`. HEAD = v0.7.2.

## 1. S-3 calibration (3+ Aces "almost always Sun")

**Magic number bumped: `+12 → +15`** for the 3-Ace Sun bonus.

- Before: `if aceCount >= 3 then sun = sun + 12 end`
- After:  `if aceCount >= 3 then sun = sun + K.BOT_SUN_3ACE_BONUS end` (=15)

Rationale per commit body + CHANGELOG: Wave-2 audit found 3-Ace hands without an AKQ stopper landed at sun~41 vs thSun=44-56 — couldn't fire R1 reliably. +15 puts the floor at sun~44, crossing thSun in ~70% of jitter outcomes (vs ~30% under +12).

## 2. Magic-number K.* promotion — full list

Seven new constants in Constants.lua:311-325:

| Constant | Value | Source patch |
|---|---|---|
| `K.BOT_SUN_3ACE_BONUS` | 15 | S-3 (was inline 12) |
| `K.BOT_SUN_MARDOOFA_BONUS` | 5 | S-8 |
| `K.BOT_SUN_MARDOOFA_PAIR_CAP` | 2 | S-8 cap |
| `K.BOT_BIDDING_SUN_OVER_HOKM_MARGIN` | 5 | B-5 |
| `K.BOT_ASHKAL_DIRECT_SUN_PIVOT` | 85 | A-6 |
| `K.BOT_PICKBID_BELOTE_BONUS` | `K.MELD_BELOTE` (=20) | B-6 (aliased) |
| `K.SUN_BEL_CUMULATIVE_GATE` | 100 | E-1 / R.CanBel |

## 3. K.* presence + correct usage in Bot.PickBid

All seven constants present at HEAD (Constants.lua:311-325). All call-sites verified at HEAD:

- Bot.lua:1059 — `K.BOT_SUN_3ACE_BONUS` (S-3)
- Bot.lua:1060-1061 — `K.BOT_SUN_MARDOOFA_PAIR_CAP`, `K.BOT_SUN_MARDOOFA_BONUS` (S-8)
- Bot.lua:1188 — `K.BOT_ASHKAL_DIRECT_SUN_PIVOT` (A-6)
- Bot.lua:1239, 1263 — `K.BOT_PICKBID_BELOTE_BONUS` (B-6, both call-sites)
- Bot.lua:1280 — `K.BOT_BIDDING_SUN_OVER_HOKM_MARGIN` (B-5)
- Rules.lua:497 — `K.SUN_BEL_CUMULATIVE_GATE` (E-1)
- Bot.lua:1114 — `K.SUN_BEL_CUMULATIVE_GATE` also picked up by Bel-fear logic

No remaining inline `12`/`+5`/`>=85`/`+20` literals at the documented sites.

## 4. Does +15 match video #25?

**Yes.** Video #25 R12 ranks 3-Ace as Definite ("ما يبقى لها كلام" — no further discussion). decision-trees.md:49 marks "3+ Aces" Definite, source video 25. v0.7.2 audit (16_pickbid_sun.md:18) records the active value as `+15 (K.BOT_SUN_3ACE_BONUS)` — matches Constants.lua:311. Calibration math (sun~44 floor vs thSun=44-56) consistent with the "almost always Sun" doc rank.

## 5. Shadowing / conflicts

No genuine conflict. Three same-value coincidences, all intentional:

- `K.MELD_SEQ5 = K.MELD_CARRE_OTHER = K.SUN_BEL_CUMULATIVE_GATE = 100` — three separate domains (meld, meld, score-gate); same number, different purpose.
- `K.BOT_PICKBID_BELOTE_BONUS = K.MELD_BELOTE` — explicit alias (commented as "mirrors meld scoring"), so retuning either tracks together. Intentional, not accidental.
- `K.BOT_ASHKAL_TH = 65` and `K.BOT_ASHKAL_DIRECT_SUN_PIVOT = 85` — these encode the doc's "65/85 pivot" (A-6) — distinct thresholds, no shadow.

Clean promotion.
