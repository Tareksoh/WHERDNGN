# 14 — v0.7.2 verification (commit 93b1e9d)

Verifying CHANGELOG claims against `git show 93b1e9d -- Bot.lua tests/test_*.lua`. All 292/292 tests pass on HEAD.

## 3a. Section 4 rule 1A — REVERT of v0.5.11 highestByRank

**PASS — true revert, not just a doc marker.**

`Bot.lua` 2512-2529: the entire v0.5.11 block (originally at this location) is GONE — only the comment header remains. The block's executable body that built `follow` and called `highestByRank(follow, contract)` was deleted. Control falls straight through to the v0.5.14 Tanfeer-sender at 2531+, then `lowestByRank(legal, contract)` at the function bottom. No "documentation-only branch that still runs" — the diff shows `-` lines for the entire `if contract.type == K.BID_SUN ... return highestByRank(follow, contract) ... end` body.

## 3b. Section 4 rule 1B — second-lowest for partner-winning + can't-beat

**PASS on the canonical fixture; one wording issue + one masking concern.**

Code at `Bot.lua` 2343-2358: inside `if partnerWinning then`, after smother/Tahreeb-sender. Filters `legal` by `C.Suit(c) == trick.leadSuit`, requires `#follow >= 2`, sorts ascending by `C.TrickRank`, returns `sorted[2]`. Test E.6 (lines 919-965) exercises seat 2 with {KH,9H,8H} after partner-led AH → returns 9H. Verified.

**Wording mismatch:** CHANGELOG says "Sun + partner-winning + must-follow + **can't-beat**" but the code has NO `wouldWin`/winners check before returning second-lowest. In `partnerWinning`, the bot never tries to beat partner — so any in-suit card with rank > partner's would still be wasted. Practically harmless (you don't overrun partner) but the comment is misleading.

**Masking edge case:** if partner is winning with a low rank (e.g. KH) and we hold AH+9H+8H, smother counts only ONE point card (AH; pointCards = {A,T,K,Q,J} of led — but partner's K isn't in our legal). With `#pointCards = 1`, completed=0, not lastSeat → smother gate fails → 1B fires → second-lowest = 9H, **and AH is silently retained** instead of being smother-donated. The Takbeer gate's `≥2` threshold predates 1B; pairing them lets a single A/T slip past donation in early tricks.

## 3c. Section 11 rule 1 wire — K/T loses → void

**PASS for "K/T loses at observation time"; FAIL for late-trick overrules.**

Code at `Bot.lua` 319-355. Iterates `s_trick.plays` looking for any play with higher TrickRank than the K/T just observed. Correctly skips `p.seat == seat`. Gated to `BID_SUN`, lead suit follow, not illegal.

**Edge case missed:** `OnPlayObserved` fires once at the moment of play. If seat 2 plays KH and the trick currently has only `{seat=1, card=JH}`, the K does NOT lose at this moment (lost=false). Later seat 3 plays AH and overrules — the void flag is NEVER set retroactively. So K-loses-to-later-A is silently dropped. The wire only catches the case where the higher card was played BEFORE the K/T (i.e. the lead was an A, or earlier seat in same trick beat them).

## 3d. Tests

**Partial.** E.1 updated (KH → 8H) and E.6 added for rule 1B; both pass. **No test for the Section 11 rule 1 wire** — neither the win-case (K wins → void NOT set) nor the loss-case (K loses → void set) is asserted. CHANGELOG only claims "+1 new E.6", so this is consistent with the changelog but the wire is unpinned.

## 4. Other edge cases the diff missed

- 1B fires even when `lastSeat=true` after smother fails (e.g. only J/9 in suit at pos 4); could be intentional but not stated.
- 1B and Faranka (line 2150) don't conflict (Faranka requires A+cover); confirmed safe.
- Section 11 wire's exclusion of Q/A is documented but Q-loss-detection would be cheap to add later if confidence improves.
