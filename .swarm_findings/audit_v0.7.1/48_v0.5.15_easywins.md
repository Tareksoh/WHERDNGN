# v0.5.15 — easy-wins batch (UI gate + Ashkal test fixture + doc refresh)

Commit 533023a. Three concurrent items: UI Bel button gate, Ashkal eligibility fixture, doc refresh. Plus a Sun-overcall investigation that found NO bug.

## 1. UI Bel button gate on R.CanBel — actively gating

**Verdict: actively gating, not cosmetic.** UI.lua:1701-1734 (PHASE_DOUBLE render). Computes `canBel = R.CanBel(R.TeamOf(S.s.localSeat), S.s.contract, S.s.cumulative)`. If `canBel == false`, renders disabled placeholder `"|cff999999Bel forbidden (Sun >=100)|r"` with no-op handler PLUS Skip. Otherwise renders the normal Bel/Bel-open/Bel-closed confirm-actions. The handler on the placeholder is `function() end` — clicking it does nothing, but it's not an `addConfirmAction` so it's purely informational. Net.LocalDouble's R.CanBel guard remains the wire-side enforcement; the UI gate just makes the forbidden state visible instead of silently swallowed.

## 2. Ashkal eligibility fixture — present, 16 cases, full 4×4 matrix

**Verdict: confirmed.** tests/test_state_bot.lua line 1079, "Section G. Ashkal eligibility (audit-recommended fixture)". Loops `for dealer = 1, 4 do` × `for seat = 1, 4 do` = exactly 16 `assertEq` calls. Each case sets up bids (HOKM:H from order[1], ASHKAL from `seat`, PASS others), invokes `S.HostAdvanceBidding`, and asserts `(action=="contract" and payload.type==K.BID_SUN)` matches the local `ashkalEligible(dealer,seat)` helper.

## 3. Pins v0.5.7's correct restriction — yes

**Verdict: yes.** The `ashkalEligible` helper computes bid order = `{(d%4)+1, ((d+1)%4)+1, ((d+2)%4)+1, d}` (dealer's-left first, dealer last in 4-bid round) and returns `i >= 3`. Position 3 = dealer's-LEFT, position 4 = dealer. This is the post-v0.5.7 correct behavior, opposite of v0.5.6's dealer+dealer's-RIGHT inversion.

## 4. Doc refresh — glossary line numbers + decision-trees rule statuses

**glossary.md** — re-anchoring section updated to v0.5.15 (was v0.5.4 dab7ee1). Added "Current snapshot (v0.5.15)" table with 19 symbol→line entries (PickBid 890, pickLead 1289, pickFollow 1882, PickDouble 2403, PickSWA 2746, etc.). Notes drift of +165 to +461 lines across v0.5.8→v0.5.14.

**decision-trees.md** — Section 1-7 headers re-anchored to current line numbers (PickBid 725→890, PickDouble 1787→2403, PickSWA 2120→2746, pickLead 953→1289, pickFollow 1457→1882, PickAKA 1686→2302). HTML comment warns cell-level MAPS-TO refs are NOT updated. S6-7 partner-winning-trick row: stale "may need exception" claim removed; replaced with "ALREADY WIRED: Rules.lua:118-121 + 147-149", and re-flagged actual remaining gap (pickFollow heuristic to *prefer* non-trump discard).

## 5. Inversion-bug catch

**Verdict: yes, would catch.** If v0.5.6's bug (dealer + dealer's-RIGHT eligible, i.e. positions 2+4) were reintroduced, position 3 (dealer's-left) would no longer honor Ashkal — but the helper's `i >= 3` asserts it should. The 4 dealer's-left cases would all flip from `expected=true, actual=false`, producing 4 failures. The seat-1 special case (seat IS order[1]) routes through a divergent bid setup but still asserts `expected=false`, so it doesn't mask the inversion.

wrote to C:/CLAUDE/WHEREDNGN/.swarm_findings/audit_v0.7.1/48_v0.5.15_easywins.md
