# v0.5.22 — Section 11 rule 3: Pigeonhole Pin Extension

**Commit:** 732610b | **HEAD:** v0.7.2 | **Files:** BotMaster.lua (+35), CHANGELOG.md (+50)

## 1. Pin extends J/9 to all trump ranks — VERIFIED

The v0.5.0 H-1 pin (lines 271-283) only pins `J{trump}` and `9{trump}` to bidder. The v0.5.22 block (285-318) pins **all** unseen trump cards (K/Q/T/A/8/7 included) to the single non-void seat. The inner loop (`if C.Suit(u) == trump and u ~= pinCard and not meldPins[u]`) covers every rank — explicit comment at line 308 confirms intent.

## 2. Loop correctness — VERIFIED with caveat

Lines 297-305 iterate `s = 1..4`, skip `seat` (self), and collect seats whose `_memory[s].void[trump]` is falsy into `trumpEligible`. Pin fires only when `#trumpEligible == 1` (line 310). Correct pigeonhole logic.

**Caveat:** The check excludes only `seat` (sampler perspective), not seats whose hands are already known (e.g., dummy hands during Tahreeb). For standard Hokm rollouts this is fine.

## 3. Edge case — partner void, opponent eligible — VERIFIED

If 2 non-self seats are non-void in trump → `#trumpEligible == 2` → **pin does NOT fire**. The block is asymmetric to seat identity (no partner/opponent distinction); it only fires at the mathematical-force boundary of exactly one. So if partner is void and one opponent is void, `trumpEligible = {other_opponent}` → pin fires correctly to that opponent. Correct behavior.

## 4. Sun contract — VERIFIED safe

Line 293 guards: `contract.type == K.BID_HOKM and contract.trump`. Sun contracts (`K.BID_SUN`) skip the entire block. No false pins. (Same guard pattern as the v0.5.0 J/9 pin above it — consistent.)

## 5. Test coverage — NOT ADDED

`git show 732610b --stat` shows only `BotMaster.lua` + `CHANGELOG.md`. No `tests/` files modified. Grep for `pigeonhole|Section 11|trumpEligible` in `tests/` returns zero matches. Commit message claims "226/226 still pass" — regression coverage only, no new test asserting the K/Q/T/A/8/7 pin behavior. **Coverage gap.**

## Summary

Implementation is correct and matches the audit-claimed line range (285-318). All three guarded edge cases (Sun, partner-void, multi-eligible) handled correctly via the single `#trumpEligible == 1` gate. Missing dedicated test for the new branch — relies on integration-level rollout statistics rather than unit assertion.
