# v0.5.18 Takbeer Point-Card Extension — Verification

**Commit:** `22ec933` (Mon May 4 23:48:03 2026)
**Files touched:** `Bot.lua` (+21/-13), `CHANGELOG.md` (+61). **No `tests/` changes.**
**Scope:** smother branch in `pickFollow` — partner-winning + non-trump-led + feedSafe gate.

## What was extended

The candidate set in the smother branch was widened from `{A, T}` to `{A, T, K, Q, J}`:

```lua
-- before:
if C.Suit(c) == lead and (r == "A" or r == "T") then
    highInSuit[#highInSuit + 1] = c
-- after:
if C.Suit(c) == lead and (r == "A" or r == "T"
                          or r == "K" or r == "Q" or r == "J") then
    pointCards[#pointCards + 1] = c
```

Variable renamed `highInSuit` → `pointCards`. Gate (`#pointCards >= 2 OR completed >= 3 OR lastSeat`) and descending-sort + `[1]` (the v0.5.11 single-char `<→>` flip) were both preserved.

## Question-by-question

**1. Correctly added point-card prioritization?** Yes. All 5 Saudi "ابناء" point ranks (A=11, T=10, K=4, Q=3, J=2) now flow through the existing descending sort, so `[1]` returns highest among legal point cards in led suit.

**2. Trick-2/3 of Sun, partner takes with K — bot follows with A or T?** Yes — wired correctly. With KH led + partner-winning + feedSafe gate satisfied, a hand with `{AH, TH, 8H}` will sort `{AH, TH}` descending and return AH. The pre-v0.5.18 code already did this for `{A, T}`; v0.5.18 only changes behavior when no A/T is present (e.g. `{KH, 8H}` now donates KH instead of falling through to `lowestByRank` returning 8H).

**3. v0.5.11 relationship.** Builds on, does not modify. v0.5.11 was the `<→>` smother-direction fix (donate highest, not lowest). v0.5.18 re-uses that descending sort; the only change is the candidate filter. The v0.5.11 Section 4 **rule 1** dump-highest fix (separate branch in opp-winning fall-through) is **untouched** by v0.5.18.

**4. Test coverage.** None. No new fixture; commit message claims "226/226 still pass" but adds zero pinning tests for KH/QH/JH smother behavior. **Gap.**

**5. Cross-check with v0.7.2.** No conflict. v0.7.2 reverted v0.5.11's **rule 1A** (opp-winning fall-through; highest → lowest/Tasgheer) and added **rule 1B** (Sun + partner-winning + can't-beat → second-lowest). v0.5.18 lives in a **different code path** — the smother branch fires *before* the rule-1B fall-through, gated by feedSafe + partner-winning + non-trump-led. The two are orthogonal. v0.7.2 commit message references "no A/T/K/Q/J of led suit to donate" — explicitly assumes v0.5.18's expanded candidate set is upstream gate.

## Findings

- **Correctness:** sound. Single-char rank-list expansion, no logic shift.
- **Test debt:** v0.5.18 ships untested. Pin fixtures should cover {KH+x lastSeat → KH}, {QH+x lastSeat → QH}.
- **Compat with v0.7.2:** clean. v0.7.2 builds on v0.5.18 as a precondition.
